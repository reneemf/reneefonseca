# Static site — no build step. The homepage and assets live in site/.
# (Migrated off the Hugo/Apéro setup; the legacy Hugo files in the repo
#  root are no longer deployed.)
[build]
  publish = "site"
  command = ""
